Title: Plastic Promise C4 Level 1 — System Context
Focus: ownership, deployment boundary, and derived inference
Last updated: 2026-08-07

+-----------------------+       loopback HTTP / SSH LocalForward
| User / coding agent   | =========================================>
| Codex, MCP client     |                                          |
+-----------------------+                                          v
                                                        +--------------------------+
                                                        | Plastic Promise runtime  |
                                                        | MCP + governance         |
                                                        +------------+-------------+
                                                                     |
                 canonical transaction                               | derived projection
                  => one truth                                       | => rebuildable
                                                                     v
              +---------------------------+              +-------------------------+
              | SQLite WAL                |              | LanceDB generation      |
              | memory / task / audit     |              | vector + text retrieval |
              +---------------------------+              +-------------------------+
                                                                     ^
                              bounded derived inference              |
                                             =>                      |
  +--------------------------+      loopback-only / restricted tunnel|
  | Cloud provider           | <=====================================+
  | structured / embedding   |                                        |
  +--------------------------+                                        |
                                                                     |
  +--------------------------+      no SQLite, LanceDB, MCP, queue, or canonical write
  | Local heterogeneous      | <=====================================+
  | inference node           |
  +--------------------------+

+--------------------------+
| GitHub release delivery  |  Publishes immutable software evidence only.
| PR / RC / stable workflow|  It has no runtime-state or canonical-write path.
+--------------------------+

Legend:
  => synchronous call or durable transaction
  ~> asynchronous bounded work (shown in Level 2/3)
  SQLite is the only canonical truth; providers and nodes return derived results only.
