# Trae × Plastic Promise — 约定工程规则
# 本文件由 Plastic Promise 框架自动生成，CLAUDE.md 为权威来源

## 核心约定（内化于心）

1. **先查再问** — 任何操作前调用 memory_recall + context_supply，不猜测
2. **每步有 git** — 每次实质性产出必须有 commit，确保可追溯
3. **每步有闭环** — 产出后必须 step-closure(mode="full")，轻量操作用 mode="light"
4. **写前查信任** — 写文件前 defense(action="get")，trust < 0.30 时向用户确认
5. **子 Agent 必带上下文** — 派发前 memory_recall + context_supply，结果注入派发 prompt
6. **不重复造轮子** — 先查记忆，再查代码，没有再创新
7. **工具即感官** — 充分利用 MCP 工具扩展能力边界，不只靠文本推理
8. **全过程可查可透明** — 审计日志 + git 痕迹，第三方可复现
9. **上下文驱动决策** — 信息不足时标注「信息不足」，不猜测
10. **原则遗传** — 从 Plastic Promise 继承约定体系，不每次从零开始

## 操作速查

| 场景 | 必须调用的 MCP 工具 |
|------|-------------------|
| 会话开始 | memory_recall + context_supply |
| 写文件前 | defense(action="get") |
| 产出后 | step-closure(task_description="...", mode="full") |
| 派发子 Agent | memory_recall + context_supply → 注入派发 prompt |
| 只读查询 | step-closure(mode="light") |
| 决策前 | principle_activate + memory_recall |
| 修复 Bug | principle_activate(domain_hint="fixing") + memory_recall |
| 新功能 | principle_activate(domain_hint="building") + memory_recall |
| 架构设计 | principle_activate(domain_hint="designing") + memory_recall |
| 代码审查 | principle_activate(domain_hint="reflecting") + audit_run |

## MCP 工具速查

- **记忆**: memory_recall, memory_store, memory_update, memory_forget
- **上下文**: context_supply, context_graph, auto_context_inject
- **原则**: principle_activate, principle_evaluate, principle_diffuse
- **审计**: audit_run, audit_pre_check, defense
- **闭环**: step-closure, scarf_reflect, feedback_apply
- **追踪**: skill_session_start, skill_session_complete, skill_session_trace
- **管理**: issue_create, issue_transition, system(action="stats")
