# Load the atomically activated provider configuration after bootstrap values.
# Keep this optional so the control/dashboard bootstrap can start before the
# first revision is activated; the control plane itself remains the authority.
[Service]
Environment=TZ=UTC
EnvironmentFile=-/srv/plastic-promise/state/control/managed.env
